"use strict";

/*btn.addEventListener ('click', function () {

 }
 );

 separate div for buttons...how do I repeat the buttons 100x while keeping code dry?

want to hide check or x depending on if task is done, so a (if task then hide) if statement

maybe a hidden/pop-up modal window once all tasks are completed

 */
